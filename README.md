# Chaplain+ 🇺🇦

Платформа для капеланів України - веб-додаток для надання духовної підтримки військовим.

## 🚀 Особливості

- **Біблійна підтримка** - колекція біблійних віршів для натхнення
- **Молитовна підтримка** - молитовник для різних ситуацій
- **ПТСР підтримка** - матеріали з роботи з посттравматичним стресовим розладом
- **База даних SQLite** - зберігання всіх постів у базі даних
- **REST API** - повний набір endpoints для управління контентом

## 📋 Вимоги

- Node.js 18+
- npm або yarn

## 🛠️ Встановлення

1. Клонуйте репозиторій:

```bash
git clone <repository-url>
cd Chaplain+
```

2. Встановіть залежності:

```bash
npm install
```

## 🏃 Запуск проекту

### Розробка (одночасно сервер і клієнт)

```bash
npm run dev:all
```

Це запустить:

- **Backend сервер** на `http://localhost:3001`
- **Frontend додаток** на `http://localhost:5173` (або інший порт Vite)

### Окремі команди

**Тільки сервер:**

```bash
npm run server
```

**Тільки клієнт:**

```bash
npm run dev
```

## 📁 Структура проекту

```
Chaplain+/
├── server/              # Backend сервер
│   ├── index.js        # Express сервер з API endpoints
│   ├── database.js     # Підключення до SQLite
│   ├── migrations.js   # Створення таблиць БД
│   └── seed.js         # Заповнення БД початковими даними
├── src/                # Frontend React додаток
│   ├── components/     # React компоненти
│   ├── utils/          # Утиліти (API виклики)
│   └── ...
└── server/chaplain.db  # SQLite база даних (створюється автоматично)
```

## 🗄️ База даних

Проект використовує **SQLite** з трьома основними таблицями:

- `bible_posts` - біблійні вірші
- `prayer_posts` - молитви
- `ptsr_posts` - статті про ПТСР (sections зберігаються як JSON)

База даних створюється автоматично при першому запуску сервера. Початкові дані завантажуються з файлів `src/biblePost.js`, `src/prayerPost.js` та `src/data/ptsrPosts.json`.

## 🔌 API Endpoints

### Біблійні пости

- `GET /api/bible` - отримати всі пости
- `GET /api/bible/:id` - отримати один пост
- `POST /api/bible` - створити пост
- `PUT /api/bible/:id` - оновити пост
- `DELETE /api/bible/:id` - видалити пост

### Молитовні пости

- `GET /api/prayer` - отримати всі пости
- `GET /api/prayer/:id` - отримати один пост
- `POST /api/prayer` - створити пост
- `PUT /api/prayer/:id` - оновити пост
- `DELETE /api/prayer/:id` - видалити пост

### ПТСР пости

- `GET /api/ptsr` - отримати всі пости
- `GET /api/ptsr/:id` - отримати один пост
- `POST /api/ptsr` - створити пост
- `PUT /api/ptsr/:id` - оновити пост
- `DELETE /api/ptsr/:id` - видалити пост

## 🏗️ Збірка для продакшн

```bash
npm run build
```

Зібраний додаток буде в папці `dist/`.

Для перегляду зібраного додатку:

```bash
npm run preview
```

## 🔧 Налаштування

### Змінні оточення

Створіть файл `.env` в корені проекту (опціонально):

```env
VITE_API_URL=http://localhost:3001/api
```

Якщо змінна не встановлена, використовується Vite proxy (налаштований в `vite.config.js`).

## 📝 Технології

- **Frontend:** React 19, Vite, Tailwind CSS, React Router
- **Backend:** Express.js, SQLite (better-sqlite3)
- **База даних:** SQLite

## 📄 Ліцензія

Всі права захищені 2025
