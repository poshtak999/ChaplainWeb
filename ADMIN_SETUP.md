# Налаштування адмін-панелі

## Пароль за замовчуванням

За замовчуванням пароль для адмін-панелі: **`admin123`**

## Зміна пароля

Для зміни пароля встановіть змінну оточення `ADMIN_PASSWORD`:

### Windows (PowerShell)

```powershell
$env:ADMIN_PASSWORD="ваш_новий_пароль"
npm run server
```

### macOS/Linux

```bash
export ADMIN_PASSWORD="ваш_новий_пароль"
npm run server
```

### Або створіть файл `.env` в корені проекту:

```
ADMIN_PASSWORD=ваш_новий_пароль
```

## Безпека

⚠️ **Важливо:**

- Ніколи не комітьте файл `.env` в Git
- Використовуйте складний пароль у продакшн середовищі
- Токени авторизації дійсні 24 години
- Після перезапуску сервера всі токени стають недійсними

## Доступ до адмін-панелі

1. Щоб перейти в адмін "/admin"
2. Введіть пароль
3. Після успішного входу ви зможете керувати постами

## Вихід з адмін-панелі

Натисніть кнопку "🚪" у Header після входу, або просто закрийте браузер (токен зберігається в localStorage).
